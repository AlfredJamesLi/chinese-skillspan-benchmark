# 手册 C — 人工标注 SOP v4（repartition_v1）

协议与手册 B **v4.2.1** 相同（短跨度、禁半词、平面 L/K/S/T、语言考试/证书为 **L**；Python 岗位用法 S、原理/课程整段 K）。重叠裁决见 `notes/handbooks/handbook_B_overlap_adjudication.md`。  
**出处：** 每条操作化定义的文献键与完整条目见 `handbook_B_citations.md`。键：[ESCO14] [ESCO-L] [Z22] [AP08] [TKS02] [FM09] [Yu20] [UD20] [D18]。`[本协议]` = 本资源操作化，不是被一篇论文强制的金标准。  
**当前主队列是原 980 分歧句**（`reports/human980_doccano/`），不是新划分 4222 的全量人标。repartition IAA-300 包仍是草稿，未开标。  
**不是** 已完成的人工 Gold。未裁决前不得写入 `confirmed-results.md`、不得覆盖 `gold_canonical_v2.jsonl`、不得覆盖 V4 hybrid 2601。  
980 SimHuman 是规则叠加，**不能**当成已经做完的双盲人标。

旧 Table 2 IAA（n=100）测的是 Gold v2 Doccano 长跨度（手册 A），与本轮 **不是同一套边界**。

## 标签

| 键 | 标签 | 标什么 | 不要标成 | 出处 |
|---|---|---|---|---|
| `l` | L | 语种、水平、语言考试/证书：英语、英语六级、CET-6、日语N2 | 把六级并进 K（Gold v2 旧规）；ISO/OCJP 才是 K | [ESCO-L] [本协议] |
| `k` | K | 学历、专业、领域知识、非语言职业认证；课表/原理里的语言名 | 岗位里的 Python / Spring（那是 **S**）；英语六级（那是 **L**） | [ESCO14] [Z22] [本协议] |
| `s` | S | 工具、方法、可执行职业技能 | 整段岗位职责 | [ESCO14] [Z22] |
| `t` | T | 软技能、特质 | 形象外貌、身体健康 | [ESCO14] [Z22] [本协议] |

快捷键与 Doccano `labels.json` 一致：`l` `k` `s` `t` [D18]。

## 跨度规则（必须）

1. 跨度是句中**连续原文**。不要改写、不要补字。[Z22] [D18]  
2. **禁止半词**：不要 `支持服`、`操作系统的问`、`培训其`。[本协议]  
3. **短而独立，优先 2–8 字**：`Python`、`计算机专业`、`沟通能力`。不要一整段职责收成一条 S。**[本协议]**（不要引 SkillSpan 当短跨度出处 [Z22]）。切段见 [AP08]。  
4. 顿号/逗号/「和、与、或」连接的**独立**能力 → **各自一条**。[Z22] 同一动词统辖的并列宾语见重叠附录 [UD20]。  
5. 「熟悉 / 掌握 / 了解 / 精通 / 具备 / 具有」**只标后面的对象**，不标这些动词。[Z22] [本协议]  
6. 编程语言、框架、办公软件、具体工具，岗位用法 → **S**。句里当课程/原理/基础/语法 → **K**，且圈完整知识短语（`Python语言原理`，不要只圈 `Python`）。[ESCO14] [本协议]  
7. 语种名、语言水平、语言考试（大学英语六级 / CET-6 / 日语N2）→ **L**。ISO / OCJP 等技术或职业认证 → **K**。L 既是知识也是能力，故单独列支，不并进 K。[ESCO-L] [本协议]  
8. 平坦、不重叠。一条跨度一个类型。不要嵌套。[TKS02] [本协议] 嵌套是另一项任务 [FM09] [Yu20]。L–K–S–T **不是**优先级。[本协议] [AP08]  
8b. 潜在同界/嵌套/交叉：能拆就拆；否则只留完整能力，备选写入裁决日志。不要猜。[AP08] [Z22] 详见手册 B 重叠附录。  
9. 不标：形象外貌、身体健康、年限数字本身（只留能力）、非司机岗驾照、留学优先套话。[Z22] [本协议]

## 空句（`spans: []`）

整句没有对候选人的能力要求 → 全部不标。[Z22] [本协议] 默认空：

- 事业单位**流程**：报名、资格审查、笔试面试时间、准考证、体检、公示、咨询电话、岗位代码、招聘人数。  
- **福利/班次**：五险一金、带薪年假、包吃住、加班费、节日福利、班次、地点。  
- 鸡汤、公司简介、工位家具、弹性上班时刻表。  
- **应届生/校招**特有：实习期长短、户口/档案、三方协议、开题答辩时间、仅「欢迎应届生」而无能力短语。

`empty_hint` 只是提示，不是命令。句中若有明确能力短语，仍要标。

## 双盲（IAA 项目）

- 标注员 **A / B** 不得看银标、不得看对方结果、不得看 JobBERT/Qwen 预测。[AP08] [Z22]  
- Doccano 导入 `iaa100_blank.jsonl` 或 `iaa300_blank.jsonl`（`label` 为空）。[D18]  
- 先做 **100 句**（与旧文 Table 2 样本量对齐），再做其余 200，合成 300。  
- 分歧由第三人对照 `adjudicator/iaa300_silver_reference.jsonl` 裁决；银标只是参考，**可以整句改空或全改**。[AP08]

## 预标复核（980 ∩ 新 test）

这是**另一条轨道**，不要和双盲 IAA 进同一个 Doccano 项目。  
预标是 v4 银标草稿，用来改错，不是答案。改完仍须按上面规则。

## 导出

Doccano 导出 JSONL，放到 `reports/repartition_v1/human_pack/exports/`（自建）。  
保留 `meta.id`。不要改 `text`。打分前把路径发回来。

## 对照例

| 句 | 应标 | 不要 | 出处 |
|---|---|---|---|
| 熟悉使用Word，Excel，PPT等办公软件 | Word S，Excel S，PPT S | 标「熟悉使用」；或收成一条长 S | [Z22] [本协议] |
| 本科及以上学历，大学英语6级 | 学历 K；六级 **L** | 六级标 K（那是 Gold v2 / 手册 A） | [ESCO-L] [本协议] |
| 维护和支持服务 | `维护` S + `支持服务` S（完整词） | `支持服` | [本协议] |
| 五险一金，带薪年假 | 空 | 硬标福利 | [Z22] [本协议] |
| 欢迎2026届毕业生加入 | 空 | 把「毕业生」标成 K | [Z22] [本协议] |
| 掌握…如 R, Python, C | `R`/`Python`/`C` 各 **S** | 举例改 K；整段长 S 再叠 K | [本协议] |
| 了解 Python 语言原理 | `Python语言原理` **K** | 只标光杆 `Python` | [ESCO14] [本协议] |
| 计算机专业课程包括 Python、数据结构 | `计算机专业` K；`Python` K；`数据结构` K | Python 标 S | [本协议] |
