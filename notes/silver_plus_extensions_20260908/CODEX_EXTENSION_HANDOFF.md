# Cursor → Codex：补跑已完成，请据此撰写 / 改稿

**把本文件整份交给 Codex。** 数字已冻结。不要再向 Cursor 要新实验，除非文稿发现数据交叉、定位系统性错误或模型来源无法核验。

工作区：`/home/guojingli3/Chinese-Skillspan-Benchmark`  
投稿：PeerJ Computer Science。含 DASFAA 的文件名只是历史草稿。  
机 B：`DS210039` / `144.214.210.39`。

评分器 `cnss-lskt-1.2.0`。Gold150 SHA256 仍为  
`ca8db0bc386c24543fec845d42ea8e24883eb67b8ce75129ac1768c5c0310fd0`。  
标准差一律为 **n=3 样本标准差，不是测试集置信区间**。

本轮替代了「含 KNN / 随机示例」的旧草案；**Qwen 只做 SFT**。1M 未跑。旧 17460 未重标。

---

## 0. 你要写什么

完成 PeerJ 稿中与 Silver-plus / Gold150 相关的方法、实验、结果、局限。允许并行改稿，不必等新 run。

**主报（不变）：** JobBERT-zh 3M、v6a、B2、Gold150 typed exact **0.5536±0.0054**（relaxed 0.6890±0.0085）。  
Challenge-100 **0.5803±0.0036**；Audit-50 **0.5071±0.0092**。  
B2 类型 exact 约 L/K/S/T = 0.867 / 0.491 / 0.555 / 0.616。

**宜写：**「新版监督方案（B2 vs B1）的观测差异，大于本轮从 1382 扩到 2156 带来的差异。」  
**不要写：**「标签质量远胜数据量」。

v5/v6b B2 0.5540±0.0041 只作「训练含 44 条与冻结 dev 同文副本」的对照。v6b 的 B2 与 v5 哈希相同，不增加新 B2 证据。

隔离口径：**句级隔离扩展版**。禁止改称文档隔离。

---

## 1. 绝对禁止

- 与历史 hybrid 2601 上 JobBERT **0.4331** 或 Qwen 零样本 SOP extract **0.1724** 相减，或放进同一主表当同一测试。
- 把完整 2601 当本轮学生的独立测试。
- 把 Gold150 多轮观察后的扩展写成全新盲测。
- 把本轮 **只用 B2** 的 Qwen 实验写成独立验证「B2 优于 B1」。
- 把相对旧 JSON-offset v3（Gold 0.0788±0.0108）的变化全部归因于数据量（本轮还改了选模：v3 取最后一轮，本轮按开发集 typed exact）。
- 把 Qwen JSON-offset SFT 写成官方 SOP extract 0.1724 的复现或替代。Qwen B0 0.0000 **不是** 0.1724。
- 把 JobBERT B0 Gold150 0.1745 写成独立测试（Gold150 ID 在 hybrid 2601 内；发布 CRF 无选模 history）。
- 把 H/E 写成客观难度真值；把一次抽样的三模型种子写成已估计抽样不确定性。
- 把 HEM（564 条、重初始化 CRF）替换 v6a 全量主数字 0.5536。
- 把 HEM 写成「继续训练发布 CRF」。发布 CRF 独立性无法证明。
- 把 1M 写成纯预训练规模消融（未跑；1M 与 3M DAPT 配方不同）。
- 只保留最优或因分数下降更换测试集。
- 引入 SpanAnchor / KNN / 随机示例 / ESCO 作为本轮正式方法（草案已废止）。

历史 0.4331 / 0.1724 若出现，只能作为 **另一测试集、另一协议** 的已发表/已复现数字，单独成表或附录。

---

## 2. 数据版本（写作时要用的清单）

| 版本 | train/dev | 说明 |
|---|---|---|
| v3 | 1382/169 | 严格文档隔离。JobBERT B1/B2 与 Qwen JSON-offset 都在此评过 Gold150 |
| v4 | 1913/169 | 句级扩展，同文档不同句进训练。只跑 JobBERT |
| v5 | 2200/169 | 再含精确重复非代表。含 44 条与冻结 **dev** 同文副本（不是 Gold 泄漏） |
| **v6a 主配置** | **2156/169** | 从 v5 train 去掉那 44 条。**论文主数字用这个** |
| v6b | 2200/132 | 从 dev 去掉重叠；B2 与 v5 相同 |
| **本轮训练清单** | **2150/169** | `v6a_nocross_20260908`：另建版本，**不改 v6a 原文件**。因 3 条整句 NFC 跨 train/dev，从 train 去掉 6 个 ID |

三条整句 NFC（不是公共片段截取；标点/模板不自动当泄漏，但整句不得跨 train/dev/test）：

1. `']` — train `1945-s0010`, `1950-s0004`, `1959-s0011`, `1960-s0025` / dev `1954-s0007`
2. `。` — train `1898-s0015` / dev `1843-s0009`
3. `关注公众号马克数据网` — train `1873-s0012` / dev `1843-s0010`

Gold150 与 train/dev：同 ID = 0。Hold-82 未进训练、未转空标。82 条过程证明：GitHub PR https://github.com/AlfredJamesLi/chinese-skillspan-benchmark/pull/1

同文档不同句：已批准的句级扩展，保留来源标记。

---

## 3. JobBERT 主实验（已有，本轮未重跑全量）

学生：JobBERT-zh 3M。v3–v6a 训练曾加载发布 `crf/best.pt`。新独立性主张 **不能** 继承该 CRF（无 `history.json`）。  
Gold150 B2 typed exact：

| 版本 | B1 | B2 |
|---|---|---|
| v3 1382/169 | 0.1390±0.0112 | 0.5299±0.0105 |
| v4 1913/169 | 0.1398±0.0033 | 0.5436±0.0031 |
| v5 2200/169 | 0.1365±0.0077 | 0.5540±0.0041 |
| **v6a 2156/169** | 0.1422±0.0138 | **0.5536±0.0054** |
| v6b 2200/132 | 0.1332±0.0052 | 0.5540±0.0041（=v5 B2） |

v3 上 B2−B1 **+0.391**（同 X 不同 Y）。v3→v6a B2 扩量约 **+0.024**。

B1/B2 是同文换标签。增益读作「手册+教师+裁定」整包，不是教师架构消融。

---

## 4. 本轮补跑：Qwen SFT only（新）

目录：`Chinese_skill_benchmark_Paper/output/silver_plus_extensions/`  
清单：`data/v6a_nocross_20260908/`（B2，2150/169）。  
模型：Qwen2.5-14B-Instruct（不是 15B；基座只读，无 adapter）。LoRA r=16, α=32, qkvo, BF16, assistant-only loss。JSON-offset：Unicode 码点、0 起始、右开；K/S/T/L。  
三种子 42/43/44 各自新建 adapter，按 **开发集 typed exact** 选 checkpoint（都选中 epoch 6）。Gold 不参与选模/提示/解析。  
无检索、无随机示例、无 ESCO、无重试、无融合。

### 开发集（仅选模，不要当论文主数字）

| 种子 | 选中 epoch | 开发 exact |
|---|---|---|
| 42 | 6 | 0.1533 |
| 43 | 6 | 0.1470 |
| 44 | 6 | 0.1638 |

### Gold150 终评（正式）

| 种子 | exact | relaxed | Challenge-100 | Audit-50 | 解析失败 | 空标误报 |
|---|---|---|---|---|---|---|
| 42 | 0.1313 | 0.4730 | 0.1339 | 0.1268 | 5 | 2 |
| 43 | 0.1203 | 0.4292 | 0.1200 | 0.1207 | 4 | 1 |
| 44 | 0.1129 | 0.4353 | 0.1133 | 0.1122 | 4 | 1 |
| **均值±SD** | **0.1215±0.0092** | **0.4459±0.0237** | **0.1224±0.0105** | **0.1199±0.0073** | | |

150/150 对齐。类型 exact 均值约：T 0.22，K 0.11，S 0.10；L 金标仅 2 条，不稳定。

**写法：**

- 可写：在更大 B2 清单上，同一 JSON-offset LoRA 的 Gold exact 约 0.12，relaxed 约 0.45；缺口主要是边界/定位，不是解析崩溃。
- 可写：与 v3 同协议族相比方向仍弱于 JobBERT；本协议不能替代官方 SOP 0.1724。
- 不可写：Qwen 已追上 JobBERT；本轮独立证实 B2>B1；0.12−0.0788 全是数据量；0.12 与 0.1724 可比。

v3 Qwen JSON-offset（1382，最后一轮，Gold150）：B2 **0.0788±0.0108** / relaxed 0.4166±0.0056；B1 0.0162±0.0056。若并列表，必须同时写清数据与选模差异。

---

## 5. 本轮补跑：H/E/M 等量对照（新）

学生：**可核验 3M DAPT 编码器 + 重新初始化任务头/CRF**（拒绝加载 `crf/best.pt`）。标签 B2。  
独特训练文本 1908；A100 固定底座 96，不混作普通 H/E。  
共同可支持偶数 **n=468** → 条件名 **`H_468` / `E_468` / `M_468`**，各 564 条。抽样种子 20260908；模型种子 42/43/44。  
3 个层无共同支持，已记录，未从开发补样本。

Gold150 typed exact：

| 条件 | 42 | 43 | 44 | 均值±SD | relaxed 均值 |
|---|---|---|---|---|---|
| `H_468` | 0.4645 | 0.4439 | 0.4539 | **0.4541±0.0103** | 0.6041±0.0096 |
| `E_468` | 0.4319 | 0.4591 | 0.4123 | **0.4345±0.0235** | 0.5776±0.0237 |
| `M_468` | 0.4564 | 0.4796 | 0.4324 | **0.4562±0.0236** | 0.6089±0.0281 |

H−E ≈ 0.020，小于 E 的样本 SD 0.024。

**限定结论：** 历史冲突来源与非冲突来源的等量比较。H/E 不是难度真值。一次抽样不估计抽样不确定性。  
**不可写：** 全量 v6a 的 0.5536 被 HEM 推翻；发布 CRF 续训；H 一定难于 E。

---

## 6. 未做（不要在文中假装做了）

- JobBERT1M × B1/B2 × 3：DAPT 阶段不可比，CRF 无选模历史。见 `JOBBERT1M_SKIP.md`。
- KNN / 随机示例 / ESCO / 多轮重试。磁盘上可能有 `knn_index/` 残留，**非正式结果**。
- 旧 1.7 万句重标。
- Qwen 的 B1 或全部历史版本补齐。

---

## 7. 建议的表格结构

**表 A（历史协议，hybrid 2601 或官方 SOP）：** JobBERT v4+jieba 0.4331；本机复现 0.433118；Qwen SOP extract 0.1724。脚注：另一测试/协议。

**表 B（本工作 Gold150，主结果）：** JobBERT v6a B2 0.5536±0.0054，加 B1、v3/v4 规模、Challenge/Audit、类型。不要把 0.4331 放进此表。

**表 C（Qwen JSON-offset，补充）：** v3 B1/B2；本轮 v6a_nocross B2-only 0.1215±0.0092。写清协议 ≠ SOP 0.1724。

**表 D（H/E/M，补充）：** `H_468`/`E_468`/`M_468`，注明 564 条、reinit CRF、结论范围。

---

## 8. 方法段必须写清的差异

- v3 Qwen：最后一轮；本轮：开发集 typed exact 选模。
- v3–v6a JobBERT 全量：曾 init 发布 CRF；HEM：编码器 + 新 CRF。
- 本轮 Qwen 训练数据 2150，不是 2156；因整句 NFC 跨 split 另建清单。
- Gold150 = Challenge-100（`gold100_page1`）+ Audit-50（`iaa50`）。

---

## 9. 关键路径

- 本轮汇总：`Chinese_skill_benchmark_Paper/output/silver_plus_extensions/EXTENSION_RESULTS.json`
- 方法与限制：`.../METHOD_AND_LIMITATIONS.md`
- 复现：`.../REPRODUCTION_MANIFEST.json`
- Qwen 协议：`.../QWEN_PROTOCOL.md`
- HEM：`.../HEM_BALANCE_REPORT.md`
- 核验包（主数字）：`.../output/silver_plus_phase4_v6_runs/CODEX_VERIFY_20260908/DELIVERABLE.md`
- v3 Qwen：`.../output/silver_plus_v3_phase1_20260908/CODEX_PHASE1_HANDOFF.md`

旧交接里「不要补跑 Qwen 扩量 / H/E/M」已被用户本轮授权覆盖；实验已完成。不要再写「这些实验未做」。

---

## 10. 给 Codex 的直接指令

1. 用 v6a B2 Gold150 **0.5536±0.0054** 作 JobBERT 主结果；写 B2 vs B1 与扩量的相对大小，不用「远胜」措辞。
2. 把本轮 Qwen SFT 与 HEM 写入补充实验，按第 7 节分表，遵守第 1 节禁令。
3. 局限写：Gold150 非全新盲测；JSON-offset ≠ SOP；发布 CRF 不可独立；HEM 一次抽样；Qwen 仅 B2；1M 未做。
4. 不要编造未给出的数字；不要把开发集 0.15 写成测试结果。
5. 改完后列出你写入正文的每一处新数字及其来源文件，便于核对。
