\begin{figure*}[htbp]
\centering
\includegraphics[width=0.98\textwidth]{figures/fig_goldv2_span_length.pdf}
\caption{Typed exact micro-F1 by reference span length under (a) the historical and (b) the standardized annotation convention. Both panels use the same identifiers but convention-specific supervision and boundary processing. The contrast diagnoses protocol sensitivity and is not a unified model ranking. Exact artifact mappings are given in Table~\ref{tab:repro-inventory}.}
\label{fig:span-length-profile}
\end{figure*}
