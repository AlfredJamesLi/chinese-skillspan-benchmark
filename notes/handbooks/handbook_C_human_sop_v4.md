# 手册 C — 人工标注 SOP v4（repartition_v1）

协议与手册 B **v4.1** 相同（短跨度、禁半词、平面 L/K/S/T、证书仍为 **K**）。重叠裁决见 `notes/handbooks/handbook_B_overlap_adjudication.md`。  
**当前主队列是原 980 分歧句**（`reports/human980_doccano/`），不是新划分 4222 的全量人标。repartition IAA-300 包仍是草稿，未开标。  
**不是** 已完成的人工 Gold。未裁决前不得写入 `confirmed-results.md`、不得覆盖 `gold_canonical_v2.jsonl`、不得覆盖 V4 hybrid 2601。  
980 SimHuman 是规则叠加，**不能**当成已经做完的双盲人标。

旧 Table 2 IAA（n=100）测的是 Gold v2 Doccano 长跨度（手册 A），与本轮 **不是同一套边界**。

## 标签

| 键 | 标签 | 标什么 | 不要标成 |
|---|---|---|---|
| `l` | L | 语种词：英语、英文、普通话 | 大学英语六级（那是 **K**） |
| `k` | K | 学历、专业、证书、领域知识 | Python / Spring（那是 **S**） |
| `s` | S | 工具、方法、可执行职业技能 | 整段岗位职责 |
| `t` | T | 软技能、特质 | 形象外貌、身体健康 |

快捷键与 Doccano `labels.json` 一致：`l` `k` `s` `t`。

## 跨度规则（必须）

1. 跨度是句中**连续原文**。不要改写、不要补字。  
2. **禁止半词**：不要 `支持服`、`操作系统的问`、`培训其`。  
3. **短而独立，优先 2–8 字**：`Python`、`计算机专业`、`沟通能力`。不要一整段职责收成一条 S。  
4. 顿号/逗号/「和、与、或」连接的**独立**能力 → **各自一条**。  
5. 「熟悉 / 掌握 / 了解 / 精通 / 具备 / 具有」**只标后面的对象**，不标这些动词。  
6. 编程语言、框架、办公软件、具体工具 → **S**。  
7. 大学英语六级等**等级证书 → K**；光秃「英文」才是 **L**。  
8. 平坦、不重叠。一条跨度一个类型。不要嵌套。L–K–S–T **不是**优先级。  
8b. 潜在同界/嵌套/交叉：能拆就拆；否则只留完整能力，备选写入裁决日志。不要猜。详见手册 B 重叠附录。  
9. 不标：形象外貌、身体健康、年限数字本身（只留能力）、非司机岗驾照、留学优先套话。

## 空句（`spans: []`）

整句没有对候选人的能力要求 → 全部不标。默认空：

- 事业单位**流程**：报名、资格审查、笔试面试时间、准考证、体检、公示、咨询电话、岗位代码、招聘人数。  
- **福利/班次**：五险一金、带薪年假、包吃住、加班费、节日福利、班次、地点。  
- 鸡汤、公司简介、工位家具、弹性上班时刻表。  
- **应届生/校招**特有：实习期长短、户口/档案、三方协议、开题答辩时间、仅「欢迎应届生」而无能力短语。

`empty_hint` 只是提示，不是命令。句中若有明确能力短语，仍要标。

## 双盲（IAA 项目）

- 标注员 **A / B** 不得看银标、不得看对方结果、不得看 JobBERT/Qwen 预测。  
- Doccano 导入 `iaa100_blank.jsonl` 或 `iaa300_blank.jsonl`（`label` 为空）。  
- 先做 **100 句**（与旧文 Table 2 样本量对齐），再做其余 200，合成 300。  
- 分歧由第三人对照 `adjudicator/iaa300_silver_reference.jsonl` 裁决；银标只是参考，**可以整句改空或全改**。

## 预标复核（980 ∩ 新 test）

这是**另一条轨道**，不要和双盲 IAA 进同一个 Doccano 项目。  
预标是 v4 银标草稿，用来改错，不是答案。改完仍须按上面规则。

## 导出

Doccano 导出 JSONL，放到 `reports/repartition_v1/human_pack/exports/`（自建）。  
保留 `meta.id`。不要改 `text`。打分前把路径发回来。

## 对照例

| 句 | 应标 | 不要 |
|---|---|---|
| 熟悉使用Word，Excel，PPT等办公软件 | Word S，Excel S，PPT S | 标「熟悉使用」；或收成一条长 S |
| 本科及以上学历，大学英语6级 | 两段都 K | 六级标 L |
| 维护和支持服务 | `维护` S + `支持服务` S（完整词） | `支持服` |
| 五险一金，带薪年假 | 空 | 硬标福利 |
| 欢迎2026届毕业生加入 | 空 | 把「毕业生」标成 K |
