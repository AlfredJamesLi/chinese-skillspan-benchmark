# Scoring-only extras (no GPU)

The official scorer is stdlib. Jieba is required for SOP CWS snap / hybrid eval.

```
jieba>=0.42.1
```

CRF fine-tune (GPU) uses `requirements-train.txt` in this repository. Do not install a parent-lab `../requirements.txt`.
