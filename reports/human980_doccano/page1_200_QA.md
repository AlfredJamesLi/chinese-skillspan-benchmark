# Human page-1 200 — QA（2026-09-03）

来源：`gold_page1_200.compact.jsonl`（sha256 `901c368a…3cd46c`）。  
映射后评分金标：`data/human_gold_page1_200.jsonl`（sha256 `fcecb522…617490`）。  
队列：980 分歧句的**前 200 条**（`human980.jsonl` 第 1–200 行 = `batch_01`–`batch_04`）。ID 与 Gold v2 / V4 hybrid **同一批**，例如 `1801-s0004`。

**不要覆盖** `data/gold_canonical_v2.jsonl`。**不要**用本文件替换 V4 hybrid 2601 主金标。

## 规模

| | |
|---|---|
| 句数 | 200（空句 10） |
| 跨度 | 806（S 497 / K 200 / T 108 / L 1） |
| 域 | 全部 **人工智能招聘** |
| 冲突类型 | span_mismatch 168 / empty_mismatch 29 / type_only 3 |
| 相对 980 预标改过 | 175 / 200（25 句与预标完全相同） |
| 与 Gold v2 跨度全同 | 28 |
| 与 V4 hybrid 跨度全同 | 10 |
| 标注员（跨度级） | Maple 486 / admin 160 / James1 160 |
| 一句多名 | 71（不是双盲 IAA，是同句补标） |

## 手册 B 质检（未改标签，只记日志）

57 / 200 句至少有一条旗标（`page1_200_QA.json`）：

| 旗标 | 条数 | 例 |
|---|---:|---|
| 跨度落在 jieba 词中 | 51 | `培训其`、`监控和管理当前服` |
| 吃进序号数字 | 5 | `技术背景2`、`的英文读写能力5` |
| 长度 >14 字 | 37 | 整段推广活动 |
| 标了熟悉/掌握等动词 | 17 | `熟悉统计语言` |

这些是**已公布的人标原样**。论文里必须写：page-1 200 是 980 队列的第一批人标分析集，**不是**已按手册 B 清干净的终裁 Gold，也**不是** V4 双盲 IAA。

## 发布节奏（投稿后）

剩余 **780** 句。计划投出后每天再公开 100 句到 GitHub（见 `RELEASE_PLAN_100_PER_DAY.md`）。
