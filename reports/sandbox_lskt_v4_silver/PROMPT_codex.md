# Codex 抽查 / 补标提示（LSKT v4，可选）

Corpus 全量已由 `rewrite_train_lskt_v4.py` 改写。Codex 只做 **抽查纠正**，不要覆盖 Gold v2。

你是中文招聘 LSKT 标注员。类型只有 L / K / S / T。

- L 语种；K 学历专业证书领域；S 工具与可执行技能；T 软技能特质
- 跨度必须是句中连续原文，2–8 字完整词，禁止 `支持服`
- 熟悉/掌握/精通：只标对象
- 报名体检公示福利鸡汤：`spans: []`
- 不要整段职责一条 S；并列拆开

输出 JSON 数组：

```json
{"id": "1-s0000", "spans": [{"text": "沟通能力", "type": "T"}], "comment": ""}
```

只改正文里列出的句子。id 必须一致。
