# 转给本机 Codex（读不到这台服务器）

Codex **没有**连 `/home/guojingli3/...`。不要让它打开服务器路径。做法：把 zip 拷到你笔记本，在 Codex 对话里 **粘贴下面提示词**，再 **附上 JSONL**（或每次贴一个 100 句分块）。

附件请用 compact 文件（`id` / `sentence` / `spans`），不要用带 `tokens`+BIO 的训练原件。

优先顺序：

1. `sample_50.jsonl` — 先试通格式  
2. `chunks_test_g2ids/test_g2ids_part_00.jsonl` 起 — 每块 100 句，共 27 块，与官方 Gold v2 同一批句子 ID  
3. 需要全量时再附 `train_compact.jsonl`（约 5MB，仍建议分块）

当前 `spans` 是 Cursor 规则银标（empty lock + 上限 8 token），不是人工 Gold。请纠正半词、过长职责、熟悉/掌握标了动词、福利/流程句不该有 span 等。

---

你是中文招聘 LSKT 标注员。用户会在本对话里粘贴或附件提供 JSONL；你读不到任何远程服务器。类型只有 L / K / S / T。标签时不要二分类。不要改、不要输出官方 Gold v2。

| 标签 | 含义 |
|---|---|
| L | 语种 |
| K | 学历、专业、证书、领域知识 |
| S | 工具、方法、可执行职业技能 |
| T | 软技能、特质 |

规则：

- 跨度必须是句中连续原文，完整词，禁止半词切断（例如不要标 `支持服`）
- 长度 2–8 个 token/字；短而独立（`Python`、`计算机专业`、`沟通能力`）
- 熟悉 / 掌握 / 精通：只标对象，不标这些动词
- 报名、体检、公示、福利、鸡汤：`spans: []`
- 不要把整段职责标成一条 S；并列拆开
- 平坦不重叠

输入每行一个对象，字段：`id`, `sentence`, `domain`, `spans`（`start/end/type/text`，半开区间 `[start,end)`）。

输出 JSON 数组，每句一个对象，**id 必须与输入一致**：

```json
{"id": "1801-s0001", "spans": [{"text": "无人机自动控制系统", "type": "S"}], "comment": "补全半词"}
```

空句：`{"id": "1802-s0009", "spans": [], "comment": "地点/无技能"}`

只改正文或附件里列出的句子。不要发明 id。纠正结果发回即可，用户会拷回服务器合并。
