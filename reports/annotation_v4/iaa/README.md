# Standardized-protocol IAA (not started)

Both annotators must see **raw text only**. Freeze A/B files and compute typed exact / relaxed / character agreement **before** adjudication.

**These files do not exist yet.** Do not copy SimHuman, V4 hybrid, or Doccano Gold into A/B projects.

Historical Table 2 (n=100, F1 0.532 / 0.624 / κ 0.554) is Handbook A Gold-length IAA. It is **not** v4.1 IAA.

When A/B exist, put immutable raw exports here:

- `annotator_A.jsonl`
- `annotator_B.jsonl`

and record SHA256 in `manifests/annotation_assets.csv` with status `pre-adjudication`.
