# 调用模型前的授权闸 — 待完成

**全量 7,936 仍未授权。** 2026-09-13 已用 `silver_public_api_v1.0` + `gpt-6-astra` 测通调用程序，并启动 **2500 句**（250 批 × 10）标注；标注完成后自动训 JobBERT 学生并与 v6a **0.5536±0.0054** 对照。其余新增句仍无 AI 标签，150 条人工检查任务也尚未抽取。

## 待标注数量

- 句子：7,936（仅新增 Silver）
- 批次：397 × 最多 20 句（最后一批不足 20）
- 输入：`annotation_input/batches/batch_0001.jsonl` … `batch_0397.jsonl`
- 系统提示词：`annotation_input/PROMPT_silver_plus_v4212_rev2.txt`（与已执行 2351 条同一 rev2）
- 人工手册：B.sop_v4.2.14（R23 未写入该机器提示词；不得声称本批生成使用了 v4.2.14）
- 字段：只改 `gpt6_label` / `gpt6_why` / `model` / `gpt6_remark`；`model` 如实自报，未知填 `unknown`

## 拟用模型（请择一授权）

| 选项 | 理由 |
|------|------|
| A. 与 2351 条 Silver-plus 同一调用通道 | 标签分布最可比；执行器须写下**实际**模型 ID，不得因字段名写成 GPT-6.0 |
| B. gpt-5.6-terra | 与 Gold150 共同指南附录同一展示名；仍须另存运行记录 |
| C. 用户指定的其它兼容 JSONL 的抽取模型 | 须能输出 `[[start,end,L/K/S/T], ...]` |

本准备包**不**默认开跑。

## 费用粗估（授权前）

提示词约 5–8k tokens；每句输入约 50–150、输出约 150–400（视跨度多少）。397 批合计大约：

- 输入约 2.5–4.5M tokens
- 输出约 1.5–3.5M tokens

按公开标价量级（仅供预算，不是账单）：

- 若按 GPT-4o 类：$2.50 / 1M 输入 + $10 / 1M 输出 → 大约 **USD 25–50**
- 若走实验室已有按量通道：以该通道单价 × 上列 token 为准

实际费用取决于授权模型、是否缓存系统提示词、以及空标比例。**请回复授权选项（A/B/C）与预算上限后再调用。**

## 明确未做

- 未训练、未重跑评测、未提交、未公开发布
- 未改论文数字、confirmed-results、DATA_AVAILABILITY
- 未把 150 条升格为独立盲标 Gold
